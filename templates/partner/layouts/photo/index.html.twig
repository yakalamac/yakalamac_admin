
{% extends 'partner/base.html.twig' %}

{% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('plugins/timepicker/css/jquery.timepicker.min.css') }}">
        <link href="{{ asset('plugins/fancy-file-uploader/fancy_fileupload.css') }}" rel="stylesheet">
{% endblock %}
{% block body %}
    <!-- Modal -->
    <div class="modal fade" id="partner_fancy_file_upload_image" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h3 class="modal-title">Toplu Fotoğraf Ekleyin</h3>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="row">
                        <div class="card">
                            <div class="card-body">
                                <input id="partner_fancy_file_upload_image_input" type="file" name="files"
                                       accept=".jpg, .png, image/jpeg, image/png" multiple>
                            </div>
                            <div class="card-footer row">
                                <div class="col-6">
                                    <button class="btn btn-success col m-1 text-center flex justify-content-center align-items-center"
                                            id="fancy_file_upload_save_all_button">
                                        <span class="material-symbols-outlined m-auto">save</span>
                                    </button>
                                </div>
                                <div class="col-6">
                                    <button class="btn btn-danger col m-1 flex flex"
                                            id="fancy_file_upload_remove_all_button">
                                        <span class="material-symbols-outlined center">delete</span>
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" id="button-close" class="btn btn-grd btn-grd-deep-blue"
                            data-bs-dismiss="modal">Kapat
                    </button>
                    <button type="submit" id="button-submit" class="btn btn-grd btn-grd-royal d-none">Kaydet</button>
                </div>
            </div>
        </div>
    </div>
<div class="page-breadcrumb d-none d-sm-flex align-items-center mb-3">
    <div class="breadcrumb-title pe-3">İşletme</div>
    <div class="ps-3">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb mb-0 p-0">
                <li class="breadcrumb-item"><a href="javascript:void(0);"><i class="bx bx-home-alt"></i></a>
                </li>
                <li class="breadcrumb-item active" aria-current="page">Fotoğrafları</li>
            </ol>
        </nav>
    </div>
</div>

<div class="card mt-4">
    <div class="card-body">
        <div class="row">
            <div class="col"><h5 class="mb-4">Fotoğraflar</h5></div>
            <div class="col-3 text-end">
                <button class="btn btn-primary w-100"
                        id="button-photo-partner-bulk"
                        data-bs-toggle="modal"
                        data-bs-target="#partner_fancy_file_upload_image"
                >Toplu Fotoğraf Ekle
                </button>
            </div>
        </div>
        <br>
        <div id="photo-storage" class="row g-3 justify-content-center">
            <script type="module">
                import { apiGet, apiDelete } from '{{ asset('script/modules/api-controller/ApiController.js') }}';
                const pnonce = Date.now().toString();
                $(document).ready(async ()=>{
                    const photoBuilder = photo=>`
                <div class="col-6 col-md-4 col-lg-3" id="${photo.id ?? 'photo'}">
                    <div class="card border-0 shadow-sm rounded-3 overflow-hidden">
                        <div style="height: 300px; background: #000;">
                            <img src="${photo.path}?width=200&height=200" alt="${photo.altTag ?? 'No alt-tag'}" class="w-100 h-100 object-fit-cover">
                        </div>
                        <div class="card-body p-2">
                            <input type="text" class="form-control form-control-sm mb-1 fw-semibold text-truncate"
                                   id="${photo.id}-title" placeholder="Başlık" value="${photo.title ?? ''}">
                            <input type="text" class="form-control form-control-sm mb-1 text-truncate"
                                   id="${photo.id}-altTag" placeholder="Alt Etiket" value="${photo.altTag??''}">
                            <small class="text-muted d-block">📅 ${photo.createdAt??''} &emsp; | &emsp; 🔄 ${photo.updatedAt??''}</small>
                        </div>
                        <div class="d-flex gap-1 p-2">
                            <button class="btn btn-success btn-sm flex-fill fw-semibold ${pnonce}-photo-update-btn" data-photo-id="${photo.id}">
                                <i class="fa fa-trash me-1"></i> Güncelle
                            </button>
                            <button class="btn btn-danger btn-sm flex-fill fw-semibold ${pnonce}-photo-delete-btn" data-photo-id="${photo.id}">
                                <i class="fa fa-trash me-1"></i> Sil
                            </button>
                        </div>
                    </div>
                </div>
            `;

                    apiGet(`/_json/place/${window.activePlace?.pid}/photos`, {
                        success: (success)=>{
                            Array.from  (success['hydra:member'])
                                .forEach(
                                    member=>$('#photo-storage').append(photoBuilder(member))
                                );

                            $(`button.${pnonce}-photo-update-btn`).on('click',function () {
                                toastr.warning('Daha bu özellik eklenmedi.')
                            });

                            $(`button.${pnonce}-photo-delete-btn`).on('click', function (){
                                const id = $(this).data('photo-id');

                                if(id === undefined || id === null || id === 'undefined' || id === 'null') {
                                    toastr.error('Fotoğraf silinirken bir hata oluştu.');
                                    throw new Error('No valid id');
                                }

                                apiDelete(`/_json/place/photos/${id}`, {
                                    success:r=> {
                                        console.log(r);
                                        $(this).closest('div[data-image-container]').remove();
                                        window.location.reload();
                                    },
                                    error: e=>{
                                        console.error(e);
                                        $(this).prop('disabled', false);
                                    },
                                    failure: f=>{
                                        console.error(f);
                                        $(this).prop('disabled', false);
                                    }
                                });
                            });
                        }
                    });
                });
            </script>
        </div>
    </div>
</div>
{% endblock %}
{% block javascripts %}
    <script src="{{ asset('plugins/sortable/js/sortable.min.js') }}"></script>
    <script src="{{ asset('plugins/timepicker/js/jquery.timepicker.min.js') }}"></script>
    <script src="{{ asset('plugins/fancy-file-uploader/jquery.ui.widget.js') }}"></script>
    <script src="{{ asset('plugins/fancy-file-uploader/jquery.iframe-transport.js') }}"></script>
    <script src="{{ asset('plugins/fancy-file-uploader/jquery.fileupload.js') }}"></script>
    <script src="{{ asset('plugins/fancy-file-uploader/jquery.fancy-fileupload.js') }}?v=1"></script>
    <script type="module" src="{{ asset('script/partner/place/edit.js?v=9') }}"></script>
{% endblock %}