{% macro switch_option(id, label, checked) %}
    <div class="form-check form-switch switch-option">
        <input class="form-check-input" type="checkbox" role="switch" id="{{ id }}" {% if checked %}checked{% endif %}>
        <label class="form-check-label" for="{{ id }}">
            {{ label }}
        </label>
    </div>
{% endmacro %}

{% import _self as macros %}

{% if options is defined and options %}
    <div class="row">
        <div class="col-3">
            {{ macros.switch_option('option-allows-dogs', 'Köpekler girebilir', options.allowsDogs|default(false)) }}
            {{ macros.switch_option('option-curbside-pickup', 'Araca teslim', options.curbsidePickup|default(false)) }}
            {{ macros.switch_option('option-delivery', 'Teslimat', options.delivery|default(false)) }}
            {{ macros.switch_option('option-serves-beer', 'Bira servisi', options.servesBeer|default(false)) }}
            {{ macros.switch_option('option-dine-in', 'İşletmede Servis', options.dineIn|default(false)) }}
            {{ macros.switch_option('option-editorial-summary', 'Editorial Summary', options.editorialSummary|default(false)) }}
        </div>

        <div class="col-3">
            {{ macros.switch_option('option-good-for-children', 'Çocuklar için uygun', options.goodForChildren|default(false)) }}
            {{ macros.switch_option('option-good-for-groups', 'Gruplar için uygun', options.goodForGroups|default(false)) }}
            {{ macros.switch_option('option-good-for-watching-sports', 'Spor yayını', options.goodForWatchingSports|default(false)) }}
            {{ macros.switch_option('option-live-music', 'Canlı müzik', options.liveMusic|default(false)) }}
            {{ macros.switch_option('option-takeout', 'Al götür', options.takeout|default(false)) }}
            {{ macros.switch_option('option-menu-for-children', 'Çocuklar için menü', options.menuForChildren|default(false)) }}
        </div>

        <div class="col-3">
            {{ macros.switch_option('option-serves-vegetarian-food', 'Vejetaryen servisi', options.servesVegetarianFood|default(false)) }}
            {{ macros.switch_option('option-outdoor-seating', 'Dışarda oturulacak alan', options.outdoorSeating|default(false)) }}
            {{ macros.switch_option('option-serves-wine', 'Şarap servisi', options.servesWine|default(false)) }}
            {{ macros.switch_option('option-reservable', 'Rezervasyon', options.reservable|default(false)) }}
            {{ macros.switch_option('option-serves-lunch', 'Öğle yemeği servisi', options.servesLunch|default(false)) }}
            {{ macros.switch_option('option-restroom', 'Dinlenme alanı', options.restroom|default(false)) }}
        </div>

        <div class="col-3">
            {{ macros.switch_option('option-serves-dinner', 'Akşam yemeği servisi', options.servesDinner|default(false)) }}
            {{ macros.switch_option('option-serves-desserts', 'Tatlı servisi', options.servesDesserts|default(false)) }}
            {{ macros.switch_option('option-serves-coffee', 'Kahve servisi', options.servesCoffee|default(false)) }}
            {{ macros.switch_option('option-serves-cocktails', 'Kokteyl servisi', options.servesCocktails|default(false)) }}
            {{ macros.switch_option('option-serves-brunch', 'Brunch Servisi', options.servesBrunch|default(false)) }}
            {{ macros.switch_option('option-serves-breakfast', 'Kahvaltı servisi', options.servesBreakfast|default(false)) }}
        </div>
    </div>
{% else %}
    <div class="row">
        <div class="col-3">
            {{ macros.switch_option('option-allows-dogs', 'Köpekler girebilir', false) }}
            {{ macros.switch_option('option-curbside-pickup', 'Araca teslim', false) }}
            {{ macros.switch_option('option-delivery', 'Teslimat', false) }}
            {{ macros.switch_option('option-serves-beer', 'Bira servisi', false) }}
            {{ macros.switch_option('option-dine-in', 'İşletmede Servis', false) }}
            {{ macros.switch_option('option-editorial-summary', 'Editorial Summary', false) }}
        </div>

        <div class="col-3">
            {{ macros.switch_option('option-good-for-children', 'Çocuklar için uygun', false) }}
            {{ macros.switch_option('option-good-for-groups', 'Gruplar için uygun', false) }}
            {{ macros.switch_option('option-good-for-watching-sports', 'Spor yayını', false) }}
            {{ macros.switch_option('option-live-music', 'Canlı müzik', false) }}
            {{ macros.switch_option('option-takeout', 'Al götür', false) }}
            {{ macros.switch_option('option-menu-for-children', 'Çocuklar için menü', false) }}
        </div>

        <div class="col-3">
            {{ macros.switch_option('option-serves-vegetarian-food', 'Vejetaryen servisi', false) }}
            {{ macros.switch_option('option-outdoor-seating', 'Dışarda oturulacak alan', false) }}
            {{ macros.switch_option('option-serves-wine', 'Şarap servisi', false) }}
            {{ macros.switch_option('option-reservable', 'Rezervasyon', false) }}
            {{ macros.switch_option('option-serves-lunch', 'Öğle yemeği servisi', false) }}
            {{ macros.switch_option('option-restroom', 'Dinlenme alanı', false) }}
        </div>

        <div class="col-3">
            {{ macros.switch_option('option-serves-dinner', 'Akşam yemeği servisi', false) }}
            {{ macros.switch_option('option-serves-desserts', 'Tatlı servisi', false) }}
            {{ macros.switch_option('option-serves-coffee', 'Kahve servisi', false) }}
            {{ macros.switch_option('option-serves-cocktails', 'Kokteyl servisi', false) }}
            {{ macros.switch_option('option-serves-brunch', 'Brunch Servisi', false) }}
            {{ macros.switch_option('option-serves-breakfast', 'Kahvaltı servisi', false) }}
        </div>
    </div>
{% endif %}

<style>
    .switch-option:hover,
    .switch-option *:hover {
        cursor: pointer;
        font-weight: bolder;
    }
</style>
